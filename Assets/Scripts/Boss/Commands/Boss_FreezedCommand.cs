using UnityEngine;

public class Boss_FreezedCommand : Boss_Command
{

    public Boss_FreezedCommand(Boss boss, string nameCommand, float executeTime) : base(boss, nameCommand, executeTime)
    {
    }
}
