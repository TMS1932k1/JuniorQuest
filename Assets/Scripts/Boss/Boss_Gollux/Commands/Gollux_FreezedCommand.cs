using UnityEngine;

public class Gollux_FreezedCommand : Boss_Command
{
    public Gollux_FreezedCommand(Boss boss, string nameCommand, float executeTime) : base(boss, nameCommand, executeTime)
    {
    }
}
